import App from "./components/App";

/* here our entry file is importing this above file
it will run this code
const appDiv = document.getElementById("app");
render(<App/>, appDiv); 
It will render this App component inside the index.html template*/


// now we need to run that script that put inside of our package json
