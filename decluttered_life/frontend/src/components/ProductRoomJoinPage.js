import React, { Component } from "react";

export default class ProductRoomJoinPage extends Component {
    constructor(props){ // make a constructore, it will take props
        super(props);
    }

    render() {
        return <p>Welcome to DeclutteredLife Product Room Join Page</p>;
        //return <h1>{this.props.name}</h1>; if we used props
    }

}